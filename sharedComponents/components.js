import { Components } from '@whppt/nuxt/lib/plugins/Components';

import { imageComponent, tallImageComponent, youtube, blockContent, tip } from './definitions/index';

export default [imageComponent, tallImageComponent, youtube, blockContent, tip, ...Components];
